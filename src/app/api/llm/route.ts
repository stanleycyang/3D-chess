// This file is no longer needed as we're using the AI SDK directly from the client
// The OpenAI API key is securely handled by the AI SDK

// Adding a dummy export to make this a valid module
export const config = {
  runtime: "edge",
};
